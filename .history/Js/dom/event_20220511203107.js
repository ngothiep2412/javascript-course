// 1. Sự kiện là gì ? (event)
// 2. Thêm 1 sự kiện: selector.addEventListener("eventName", handler, [optional])
// click, keydown, keypress, keyup, mousemove, mouseleave, load, DOMContentLoaded,...
// handler: function
// capture, bubbling
