window.addEventListener("load", function () {
  // Variable declaration
  const form = document.qureySelector(".todo-form");
  form.addEventListener("submit", function (e) {
    e.preventDefault();
    const total = this.elements["todo"];
  });
});
